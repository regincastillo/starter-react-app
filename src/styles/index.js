import { styled } from "@mui/material/styles";

export const MainWrapper = styled("div")({
    width: "100%",
    padding: "15px 20px",
    height: "100vh",
     display: "flex",
     alignItems: "center",
     justifyContent:"center"
  });
